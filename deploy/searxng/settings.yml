use_default_settings: true

general:
  instance_name: "FusionSearch LibreSearch"

search:
  safe_search: 0
  autocomplete: ""
  formats:
    - html
    - json

server:
  secret_key: "change-this-in-production"
  limiter: false
  image_proxy: false
  method: "GET"

ui:
  static_use_hash: true
